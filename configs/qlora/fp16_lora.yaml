quantization: null 

lora:
  r: 8
  alpha: 16
  dropout: 0.05
  target_modules: ["q_proj", "v_proj"]

training:
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 4
  learning_rate: 1e-4
  num_train_epochs: 1 
  warmup_ratio: 0.03